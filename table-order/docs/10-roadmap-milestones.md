# 10. 로드맵 & 마일스톤

- 버전: v0.1 (2026-07-12)
- **이 문서의 체크박스가 프로젝트 진행 상태의 SSOT다.** 오케스트레이터는 마일스톤 마감 시(CLAUDE.md 프로토콜 ⑦) 여기를 갱신·커밋한다.
- 기간은 에이전트 오케스트레이션 기준 캘린더 추정(사람 리뷰 대기 포함). 총 **7~8주 → 베타**.

## 진행 현황

- [x] 설계 문서 v0.1 (docs/01~11) — 2026-07-12
- [x] 레퍼런스 반영 v0.2 (룩북 3층 구조·AI 연출컷·docs/12) — 2026-07-12
- [x] 설계 검증 v0.3 — 5렌즈 49건 비평→3심 판정 반영(UX 예산·모바일 실전·운영 현실) + 사업계획 docs/13 — 2026-07-15
- [ ] M0 스캐폴드
- [ ] M1 계약·코어 도메인
- [ ] M2 룩북 메뉴판 + 메뉴 관리
- [ ] M3 주문 파이프라인 + POS 보드 (실시간)
- [ ] M4 정산·결제
- [ ] M-AI AI 연출컷 스튜디오 (병렬 트랙 — M2 이후 착수 가능, M6 전 완료)
- [ ] M5 SaaS 온보딩·구독·통계
- [ ] M6 하드닝·베타 런칭

## M0 — 스캐폴드 (0.5주)

| | |
|---|---|
| 목표 | 모노레포 골격 + CI 파이프라인 + 배포 연결 |
| 투입 | 오케스트레이터 직접 (+ `db-schema` Supabase 로컬 셋업) |
| 산출물 | pnpm+Turborepo, apps/web(Next.js 15, Tailwind v4, shadcn 초기화), packages/db·shared·ui 빈 골격, ESLint(테넌트 가드 규칙 포함)·Prettier·Vitest·Playwright 설정, `.env.example`, GitHub Actions(typecheck/lint/test), Vercel 연결 |
| DoD | `pnpm dev` 기동, `pnpm typecheck && lint && test` 그린, 프리뷰 배포 URL 생성 |

## M1 — 계약·코어 도메인 (1주)

| | |
|---|---|
| 목표 | 병렬 개발의 토대: 스키마·계약·토큰·인증 골격 확정 |
| 투입 | `db-schema` ∥ `design-system` ∥ `auth-tenancy` → 이후 `backend-api` |
| 산출물 | Prisma 스키마 전체(docs/03) + 마이그레이션 + **데모 시드**, Zod 계약 전체(docs/04 §6), 디자인 토큰·타이포 시스템·기본 컴포넌트(Button/Sheet/Card/Chip/Stepper), Supabase Auth 연동·middleware 테넌트 해석·requireStaff/requireTable, repository 레이어 골격 |
| DoD | 시드로 데모 매장 조회 가능, 계약 테스트(문서 예시↔Zod) 그린, 교차 테넌트 가드 테스트 그린 |

## M2 — 룩북 메뉴판 + 메뉴 관리 (1.5주)

| | |
|---|---|
| 목표 | **차별점 증명**: 데모 매장이 '잡지처럼' 보인다 |
| 투입 | `backend-api`(lookbook·menu admin API) ∥ `lookbook-ui`(커버/목차/피드/상세) ∥ `pos-ui`(메뉴 관리+미리보기) ∥ `backend-api`(이미지 파이프라인) |
| 산출물 | 룩북 전 화면(커버 섹션·**차림 인덱스**·**큐레이션 스프레드(밀도 규칙·INDEX 챕터)**·**퀵담기 시트**·**풀스크린 디테일 컷** — 주문 제외: 담기까지), 진입 1왕복(`/q/[token]`)+세션 이중화, 테마 주입(세로 타이포·챕터 모티프·titleLang), 메뉴/카테고리/옵션/사진 관리(재료 리스트·displayMode 포함), QR 시트 PDF(도메인 명시 인쇄), 이미지 변형 4종+바이트 상한 변환 파이프라인 |
| DoD | **p75 LCP < 1.8s**(Slow 4G)·Lighthouse perf ≥ 85, QR 인식→첫 음식 사진 p75 < 4s(중저가 안드로이드 실기기 리허설), 시드 18메뉴 피드 ≤ 12뷰포트(밀도 실측), **브라우저 6종 스모크**(진입→담기, N-11), 룩북 미리보기 즉시 반영, 사진 없는 메뉴 폴백 |

## M3 — 주문 파이프라인 + POS 보드 (1.5주) ★ 심장

| | |
|---|---|
| 목표 | 주문→실시간 알림→상태 전이→고객 현황 E2E |
| 투입 | `backend-api`(주문·세션·호출 API) ∥ `realtime`(채널·훅·폴백) → `pos-ui`(주문 보드·호출) ∥ `lookbook-ui`(카트·전송·현황) → `qa`(E2E) |
| 산출물 | 주문 생성(스냅샷·멱등키=카트 해시·orderNo·deviceId), 상태머신 서비스(자동접수·세션 첫 주문 수동 확인 포함), TableSession 라이프사이클(EXPIRED 정리 잡), store ops 채널+알림음(통합 재알림)+DEGRADED 폴백, 주문 보드(일괄 확인·새 손님 뱃지)+**주방 모드**, 카트 지속성+전송 상태기계+품절 복구 UX, 현황 화면(`한 번 더` 재주문·`매장이 확인했어요`·영수증 뷰), 호출 병합·차단 |
| DoD | 수용 기준 A-1·A-2·A-3·A-5·**A-7(4탭)·A-8(일괄 확인)** E2E 그린, 타임아웃 후 동일 멱등키 재전송=주문 1건, 부하 스모크(1매장 30주문/분) 통과 |

## M4 — 정산·결제 (1주)

| | |
|---|---|
| 목표 | 돈이 정확히 맞는다 |
| 투입 | `payments`(토스 연동·환불·방어 규칙) ∥ `pos-ui`(테이블맵·정산) ∥ `lookbook-ui`(결제위젯 시트) → `qa` |
| 산출물 | 카운터 결제·세션 CLOSE·COMPLETED 전이, 선결제 시퀀스(docs/08 §3)·웹훅·자동 부분환불, 혼합 세션 정산, PENDING_PAYMENT 표현 확정(보류 결정 해소), **선결제 인앱브라우저 분기+customerKey 재사용+SDK prefetch**, **세션 테이블 이동(F-24)**, 일 마감 리포트·기존 포스 병행 요약 |
| DoD | A-4 + A-9(테이블 이동) + docs/08 §6 필수 시나리오 전부 그린, 금액 위조 공격 테스트 그린, 인앱 3종 선결제 스모크(N-11) |

## M-AI — AI 연출컷 스튜디오 (1주, 병렬 트랙)

| | |
|---|---|
| 목표 | 재료만 디테일하게 넣으면 화보급 **실사 연출컷**이 나온다 (레퍼런스 R3) |
| 의존/배치 | M2(메뉴 관리·이미지 파이프라인) 완료 후 어느 마일스톤과도 병렬 가능, M6 하드닝 전 완료 필수. 병렬 수행 시 총 일정 무변(전담 에이전트 별도), 순차 수행 시 +1주 |
| 투입 | `ai-imagery`(제공자 PoC·파이프라인·크레딧) ∥ `pos-ui`(스튜디오 UI·라벨 에디터) ∥ `lookbook-ui`(분해컷 라벨 렌더러) → `qa` |
| 산출물 | 제공자 비교 PoC 보고(docs/12 §7)와 어댑터 확정, 생성 잡 파이프라인(QUEUED→READY→SELECTED/FAILED 환불), 프롬프트 템플릿 3종(HERO/DECONSTRUCTED/CLOSEUP), 후보 선택·라벨 에디터, 크레딧 차감·환불, AI 생성 고지 배지 |
| DoD | A-6(재료 입력→적용 3분 내) 리허설 통과, 미검수 노출 차단(I-8) 회귀 그린, 데모 매장에 AI 분해컷 1종 적용·스크린샷, 생성 p95 < 60s |

## M5 — SaaS 온보딩·구독·통계 (1주)

| | |
|---|---|
| 목표 | 남이 셀프로 가입해서 개통한다 |
| 투입 | `auth-tenancy`(가입·위저드·직원·역할) ∥ `payments`(빌링·dunning) ∥ `pos-ui`(설정·통계·구독 UI) ∥ `lookbook-ui`(테마 확장) |
| 산출물 | 랜딩·가입·온보딩 5단계(+**CSV 메뉴 임포트**)·개시 체크리스트, 플랜 게이트(**FREE/BASIC/PRO 신사다리**, docs/09 §3)·'MENUBOOK' 배지·업그레이드 CTA, 빌링 크론·dunning(TRIAL 만료→FREE 강등), 통계 5위젯(객단가·추가주문 기여 포함)+월간 리포트 메일, 슈퍼어드민 기본 |
| DoD | 신규 계정으로 가입→개시→실주문까지 **무개입 통과**(리허설), 트라이얼 만료·결제 실패 시나리오 그린 |

## M6 — 하드닝·베타 (1주)

| | |
|---|---|
| 목표 | 실매장 3곳 베타 투입 가능 상태 |
| 투입 | `qa` 주도 ∥ 전 에이전트 수정 대응 ∥ 오케스트레이터 `/security-review` |
| 산출물 | RLS 2차 방어선, rate limit 튜닝, 에러 추적(Sentry)·구조화 로깅, 전 표면 접근성 점검, 운영 런북(장애 시 폴백 확인 절차), 법무 체크리스트(docs/09 §6) 소화, 백업·복구 리허설 |
| DoD | 전체 회귀 스위트 그린, 보안 리뷰 지적 0 잔존, 베타 매장 온보딩 리허설 2회 |

## 리스크 & 대응

| 리스크 | 영향 | 대응 |
|---|---|---|
| 룩북 '감성'이 사진 품질에 의존 — 매장 사진이 나쁘면 차별점 붕괴 | 높음 | 업로드 가이드·자동 보정(밝기/크롭 제안) 백로그 상향, 베타 매장은 촬영 지원 패키지로 선별 |
| Supabase Realtime 동시 연결 한도/지연 | 중간 | 폴백 프로토콜(docs/07 §4)이 1급 시민 — 실시간 죽어도 서비스 성립. 부하 테스트 M3 DoD 포함 |
| 선결제 자금 흐름 법적 구조(PG 하위가맹) | 높음 | M4 전 법무 확인(docs/09 §6). 불가 시 MVP는 후불 전용으로 출시(모드 A만) |
| **주방 전달 부재(홀 태블릿만) — 도입 거절 1순위** | 높음 | M3 주방 모드 기본 제공, 주방 프린터(CloudPRNT 계열 — 폴링형이라 서버리스와 궁합) 어댑터를 베타 전 스트레치로, 미확인 SMS 에스컬레이션 옵션(설정) |
| 피크타임 태블릿 브라우저 절전/백그라운드로 알림 누락 | 중간 | Wake Lock+통합 연속음+미확인 배지+개점 사운드 체크, 상주 모드 가이드(docs/06 §8) |
| QR 사진 유출로 원격 장난 주문 | 중간 | 새 손님 첫 주문 뱃지·세션 첫 주문 수동 확인(기본 ON)·원탭 토큰 회전·거절 시 세션 종료(docs/06 §3~4) |
| 에이전트 병렬 작업 간 계약 드리프트 | 중간 | 계약 단독 커밋 원칙 + 계약 테스트(docs/04 §5)가 게이트 |
| 가격 가설 미검증 | 중간 | 베타 3매장 무료 + 지불의사 인터뷰 후 확정 |
| AI 연출컷 품질 편차·실물 오인(표시광고 리스크) | 중간 | 검수 승인제(I-8) + 'AI 생성 이미지' 고지 배지 + 프롬프트 템플릿 고정. 품질 미달 시 분해컷 한정으로 스코프 축소 |
| AI 생성 비용 폭증 | 낮음 | 플랜별 크레딧 한도·실패 환불·rate limit, 생성 단가 상한 모니터링(docs/12 §7) |

## MVP 이후 백로그 (우선순위 순)

1. **주방 프린터(CloudPRNT/ESC-POS) 어댑터** — 베타 매장 요구 시 즉시 착수하는 스트레치(PrintJob 테이블·실패 경고 뱃지·settings 탭 포함) 2. **종이 메뉴판·배민 사진 비전 AI 메뉴 임포트**(F-26 후반 — '메뉴 30개 30분 이관') 3. 미확인 주문 SMS/알림톡 에스컬레이션(베타는 SMS 1종) 4. 테이블 QR 후불 셀프 정산(모드 C — 수익화 검토는 docs/13 §6) 5. 다국어 메뉴(F-17) 6. 포컬포인트·자동 크롭 에디터 7. 분할 결제·합석(F-18) 8. paletteMode "auto"(저조도 시간대 대응) 9. 서브도메인/커스텀 도메인(F-19) 10. KDS 고도화(주방 모드→전용 KDS) 11. 웨이팅·예약, 리뷰·쿠폰(F-20) 12. 기존 POS API 연동 13. 네이티브 앱(Capacitor)
