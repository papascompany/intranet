// M1에서 design-system 에이전트가 에디토리얼 토큰(--mb-*)과 공용 컴포넌트를 구현한다.
// 규격: docs/05 §8(터치·타이포 하한), §10(ThemeConfig v2), .claude/agents/design-system.md
export const UI_PACKAGE_STATUS = "scaffold-only (M1에서 구현)" as const;
